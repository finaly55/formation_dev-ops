# Official framework image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/node/tags/
image: node:latest

test_one:
  script:
    - npm install
    - npm install -g mocha
    - npm run test

after_script:
  - git remote add cleverapps https://$CLEVER_TOKEN:$CLEVER_SECRET@push.clever-cloud.com/app_048ffdc4.git
  - git --verbose --force push cleverapps master
